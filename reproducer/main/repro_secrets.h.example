#pragma once
/* Copy to repro_secrets.h and fill in. repro_secrets.h is intentionally not
 * shipped: the reproducer needs a real AP to associate with (Wi-Fi is a
 * required co-factor for the fault). */
#define REPRO_WIFI_SSID "your-ssid"
#define REPRO_WIFI_PASS "your-password"
